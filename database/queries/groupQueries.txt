SELECT add_group(org_id, group_name, description);

SELECT edit_group(group_id, org_id, group_name, description);

SELECT add_indigents_to_group(ARRAY[1, 2, 3], org_id, group_id);

SELECT remove_indigents_from_group(ARRAY[1, 2, 3], org_id, grp_id);

SELECT * FROM get_group_details(grp_id, org_id);

SELECT * FROM get_groups_for_organization(org_id);
